function soma(n1 = 0, n2 = 0) {
    return n1 + n2;
}

console.log(`A soma entre 2 e 5 é: ${soma(2, 5)}`);
console.log(`A soma entre 2 e # é: ${soma(2)}`);
console.log(`A soma entre # e # é: ${soma()}`);